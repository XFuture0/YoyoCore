<b><size=20>射线投射工具</size></b>

<i>UI 和 3D 世界的交互，往往就靠一条射线来连接。</i>

RayCastTool 专门处理 UI 相关的射线检测，提供了获取单个组件和获取所有组件的功能。

<b><size=16>诞生背景</size></b>

Unity 的 UI 射线检测和 3D 射线检测使用不同的系统：

<b>3D 射线检测</b>：

Physics.Raycast(ray, out hit);

<b>UI 射线检测</b>：

PointerEventData pointerEventData = new PointerEventData(EventSystem.current);
pointerEventData.position = Input.mousePosition;
List<RaycastResult> results = new List<RaycastResult>();
EventSystem.current.RaycastAll(pointerEventData, results);

UI 射线检测的代码相对繁琐，RayCastTool 对其进行了封装，让 UI 交互检测更加简洁。

<b><size=16>核心功能</size></b>

<b>获取单个 UI 组件</b>

根据位置获取指定名称或标签的第一个 UI 组件：

// 通过名称获取 Button
Button btn = RayCastTool.RayCastUIComponent<Button>(
    Input.mousePosition,
    "ConfirmButton",
    false  // false = 按名称查找
);

if (btn != null)
{
    btn.onClick.Invoke();
}

<b>获取所有 UI 组件</b>

获取某位置下的所有指定类型的 UI 组件：

List<Image> images = RayCastTool.RayCastUIComponents<Image>(Input.mousePosition);

foreach (var img in images)
{
    Debug.Log($"检测到 Image: {img.name}");
}

<b><size=16>使用方法</size></b>

<b>点击检测</b>

void Update()
{
    if (Input.GetMouseButtonDown(0))
    {
        // 检测是否点击了按钮
        Button btn = RayCastTool.RayCastUIComponent<Button>(
            Input.mousePosition,
            "",
            false
        );

        if (btn != null)
        {
            Debug.Log($"点击了按钮: {btn.name}");
        }
    }
}

<b>拖拽检测</b>

public void OnDrag(PointerEventData eventData)
{
    // 检测拖拽目标
    IDropHandler dropTarget = RayCastTool.RayCastUIComponent<IDropHandler>(
        eventData.position,
        "DropZone",
        false
    );

    if (dropTarget != null)
    {
        // 处理放置逻辑
    }
}

<b>悬浮提示</b>

void Update()
{
    // 检测鼠标下的物品槽
    ItemSlot slot = RayCastTool.RayCastUIComponent<ItemSlot>(
        Input.mousePosition,
        "",
        false
    );

    if (slot != null && slot != lastSlot)
    {
        ShowTooltip(slot.itemData);
        lastSlot = slot;
    }
}

<b>按标签查找</b>

// 获取标签为 "Interactable" 的第一个按钮
Button btn = RayCastTool.RayCastUIComponent<Button>(
    Input.mousePosition,
    "Interactable",
    true  // true = 按标签查找
);

<b><size=16>完整示例</size></b>

<b>自定义点击处理</b>

public class CustomClickHandler : MonoBehaviour
{
    void Update()
    {
        if (Input.GetMouseButtonDown(0))
        {
            HandleClick(Input.mousePosition);
        }
    }

    void HandleClick(Vector2 position)
    {
        // 尝试获取不同类型的组件
        Button btn = RayCastTool.RayCastUIComponent<Button>(position, "", false);
        if (btn != null)
        {
            btn.onClick.Invoke();
            return;
        }

        Toggle toggle = RayCastTool.RayCastUIComponent<Toggle>(position, "", false);
        if (toggle != null)
        {
            toggle.isOn = !toggle.isOn;
            return;
        }

        Slider slider = RayCastTool.RayCastUIComponent<Slider>(position, "", false);
        if (slider != null)
        {
            // 计算点击位置对应的值
            RectTransform rect = slider.GetComponent<RectTransform>();
            float normalized = (position.x - rect.position.x) / rect.sizeDelta.x;
            slider.value = Mathf.Lerp(slider.minValue, slider.maxValue, normalized);
        }
    }
}

<b>多层级 UI 检测</b>

void Update()
{
    // 获取鼠标下的所有 Image
    List<Image> images = RayCastTool.RayCastUIComponents<Image>(Input.mousePosition);

    // 按层级排序（通常列表已经是按层级排序的）
    foreach (var img in images)
    {
        Debug.Log($"检测到: {img.name}, 层级: {img.transform.GetSiblingIndex()}");
    }
}

<b><size=16>技术细节</size></b>

<b>射线检测实现</b>

public static T RayCastUIComponent<T>(Vector3 position, string name, bool isTag = false) where T : UIBehaviour
{
    List<RaycastResult> raycastResults = new List<RaycastResult>();
    PointerEventData pointerEventData = new PointerEventData(EventSystem.current);
    pointerEventData.position = position;
    EventSystem.current.RaycastAll(pointerEventData, raycastResults);

    if (raycastResults.Count == 0) return null;

    foreach (RaycastResult raycastResult in raycastResults)
    {
        if (isTag)
        {
            if (raycastResult.gameObject.tag == name &&
                raycastResult.gameObject.TryGetComponent<T>(out _))
            {
                return raycastResult.gameObject.GetComponent<T>();
            }
        }
        else
        {
            if (raycastResult.gameObject.name == name &&
                raycastResult.gameObject.TryGetComponent<T>(out _))
            {
                return raycastResult.gameObject.GetComponent<T>();
            }
        }
    }
    return null;
}

<b>依赖 EventSystem</b>

RayCastTool 依赖 Unity 的 EventSystem，确保场景中有一个 EventSystem：

<size=13>GameObject
└── EventSystem (需要这个)
    ├── EventSystem (组件)
    └── StandaloneInputModule (组件)</size>

<b><size=16>注意事项</size></b>

<size=13><b>1. 与 Graphic Raycaster 的关系</b>

UI 射线检测需要 Canvas 上有 Graphic Raycaster 组件：

<size=13>Canvas
├── Graphic Raycaster (需要这个组件)
└── ... UI 元素</size>

<b>2. 射线穿透</b>

RaycastAll 会返回所有命中的 UI 元素，如果你只想获取最上层的元素，取列表的第一个即可：

List<RaycastResult> results = new List<RaycastResult>();
EventSystem.current.RaycastAll(pointerEventData, results);

if (results.Count > 0)
{
    // 第一个就是最上层的
    GameObject topUI = results[0].gameObject;
}

<b>3. 3D 物体与 UI 的混合检测</b>

如果需要同时检测 3D 物体和 UI，需要分别使用 Physics.Raycast 和 EventSystem.RaycastAll。</size>

<b><size=16>与 MathTool 射线检测的对比</size></b>

<size=13><b>RayCastTool</b> - 用途：UI 检测 - 射线类型：EventSystem 射线
<b>MathTool</b> - 用途：3D 物理检测 - 射线类型：Physics 射线</size>

<b><size=16>技术地位和意义</size></b>

RayCastTool 是框架中专门处理「UI 交互」的工具。它简化了 UI 射线检测的代码，让 UI 交互逻辑更加清晰。

<i>在 UI 和 3D 世界的交界处，一条射线就能架起桥梁。</i>
